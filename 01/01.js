for (let ligne = "#"; ligne.length <= 7; ligne += "#") {
    console.log(ligne);
}

// déclaration de la variable et affectation en chaine de caractere #
// la boucle continue tant que la longueur de la ligne est <= 7
// # s'affiche à chaque boucles